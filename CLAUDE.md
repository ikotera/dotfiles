# 出力整形規約
- **文中 `$...$` と mermaid は禁止**。インライン数式は使えない。数式は必ず独立式にする。
- 独立数式は `$$...$$` を**単独行**に置く（文中に埋め込むと描画されない）。
- `\(...\)` `\[...\]` 単一 `$...$` は使わない（生テキスト化する）。
- `\tag{}` は描画されない前提。番号が要るなら式の外に「(1)」とテキストで書く。
- 変数説明で本来インラインにしたい記号は、文章で言い換えるか、重要な式だけ `$$...$$` の独立行にする。
- 代替として `math` 言語指定のコードフェンスも使用可（常に独立ブロック）。

例:

(1)

$$
V_{\mathrm{load}} = V_{\mathrm{oc}} - I R_{\mathrm{int}}
$$

ここで、

- 負荷時の端子電圧 V_load（単位 V）
- 開放電圧 V_oc（単位 V）
- 負荷電流 I（単位 A）
- バッテリー内部抵抗 R_int（単位 Ω）

@instructions/shared-instructions.md
